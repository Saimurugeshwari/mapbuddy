services:
  - type: web
    name: mapbuddy-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    envVars:
      - key: GMAPS_API_KEY
        value: - AIzaSyCVCujFckt1-qAV31bTMuU5Xxn2SLH6BD8
        - key: RENDER
          value: true
